import SwiftUI

struct SetupProfileView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("Setup Profile")
                .font(.largeTitle)
                .bold()
            
        }
    }
}

#Preview {
    SetupProfileView()
}
