import SwiftUI

struct PasswordResetConfirmedView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("Password Reset Confirmed")
                .font(.title)
                .bold()
        }
    }
}

#Preview {
    PasswordResetConfirmedView()
}
