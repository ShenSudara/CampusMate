import SwiftUI

struct PasswordResetView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("Password Reset")
                .font(.largeTitle)
                .bold()
        }
    }
}

#Preview {
    PasswordResetView()
}
