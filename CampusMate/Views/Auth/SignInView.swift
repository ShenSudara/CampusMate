import SwiftUI

struct SignInView: View {
    var body: some View {
        VStack(spacing: 16) {
            Text("Sign In")
                .font(.largeTitle)
                .bold()

        }
    }
}

#Preview {
    SignInView()
}
